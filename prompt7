Perform an acceptance audit of milestone M2a.

Do not implement M2b.

Do not modify files.

Do not fix issues.

Do not install or update dependencies.

The purpose of this task is to independently verify that the new `analysis.v2` model correctly represents call occurrences, unique dependencies and observed FLOW control structures.

## Reported M2a result

M2a reported:

```text
FLOW services: 24

Call occurrences:
- total: 108
- INVOKE: 68
- MAPINVOKE: 40
- resolved: 49
- unresolved: 59

Unique dependencies:
- total: 86
- INVOKES: 61
- USES_TRANSFORMER: 25
- resolved: 45
- unresolved: 41
```

For the primary webMethods Integration Server 10.15 fixture:

```text
oa.adapter.geographicAddressManagement:createGeographicAddressValidation
```

M2a reported:

```text
call occurrences: 43
unique dependencies: 25

SEQUENCE: 32
BRANCH: 27
LOOP: 2
EXIT: 9
```

Generated output:

```text
out/m2a-analysis/analysis.json
out/m2a-analysis/services/
out/m2a-analysis/graphs/dependencies.dot
```

Reported quality gates:

```text
uv run pytest: 37 passed
uv run ruff check .: passed
uv run mypy: passed
```

## Audit rules

* Independently compare parser output with the actual `node.ndf` and `flow.xml`.
* Do not rely only on existing tests or generated JSON.
* Use temporary inspection scripts only when useful.
* Do not save audit scripts into the repository.
* Do not modify Git state.
* Do not begin M2b.

## 1. Repository baseline

Run:

```text
git status --short
git log -1 --oneline
git diff --stat
git diff
```

Report:

* starting commit;
* working-tree state;
* files changed by M2a;
* untracked files;
* whether fixture files changed;
* whether M2b functionality was started prematurely.

Read completely:

```text
AGENTS.md
README.md
docs/implementation-plan.md
docs/architecture.md
docs/fixture-inventory.md
docs/fixture-provenance.md
docs/unsupported-constructs.md
```

Inspect all M2a implementation and test files.

## 2. Quality gates

Run:

```text
uv run pytest
uv run ruff check .
uv run mypy
```

Report exact commands, exit codes and results.

Also verify:

```text
tests/fixtures/checksums.sha256
```

against all tracked fixture files.

## 3. Canonical schema audit

Inspect the `analysis.v2` IR and output.

Verify:

* the schema version is explicitly present;
* M1 and M2a semantics are not mixed ambiguously;
* call occurrences and unique dependencies are separate collections;
* the model does not call occurrences “edges”;
* every occurrence has a stable ID;
* every unique dependency has a stable canonical key;
* canonical ordering is deterministic;
* no absolute local paths are present.

Report the exact canonical key used for unique dependencies.

Check whether the key includes:

```text
caller
target
dependency kind
```

and whether resolved status is deliberately excluded from identity.

## 4. Call occurrence audit

Independently verify totals directly from all active `flow.xml` files:

```text
INVOKE
MAPINVOKE
```

Compare direct XML counts with:

```text
analysis.json
Markdown metrics
reported completion metrics
```

Verify:

* 68 total INVOKE occurrences;
* 40 total MAPINVOKE occurrences;
* 108 total occurrences;
* duplicates are preserved;
* occurrence order matches source order;
* occurrence IDs are stable;
* source references point to the exact XML elements;
* `INVOKE` maps only to `INVOKES`;
* `MAPINVOKE` maps only to `USES_TRANSFORMER`.

Report any missing, duplicated or incorrectly typed occurrence.

## 5. Unique dependency audit

Independently derive unique keys from call occurrences.

Verify:

```text
86 total unique dependencies
61 INVOKES
25 USES_TRANSFORMER
```

Check:

* no duplicate canonical dependency records;
* repeated calls aggregate into one dependency;
* occurrence count equals the number of attached call occurrences;
* source-reference samples belong to real occurrences;
* resolution status is consistent for all occurrences of a dependency;
* a caller-target pair may correctly have two dependencies when both kinds occur;
* no dependency is accidentally merged solely by target name.

Report any dependency where the aggregated occurrence count is incorrect.

## 6. OAAdapter dependency audit

For:

```text
oa.adapter.geographicAddressManagement:createGeographicAddressValidation
```

independently derive:

* 43 call occurrences;
* number of unique target services;
* number of unique `INVOKES` dependencies;
* number of unique `USES_TRANSFORMER` dependencies;
* total unique dependencies.

Confirm the reported total of 25.

Check whether any target appears as both:

```text
INVOKES
USES_TRANSFORMER
```

If so, verify that two distinct dependency records are retained.

## 7. Resolution audit

Verify the reported metrics:

```text
resolved occurrences: 49
unresolved occurrences: 59

resolved unique dependencies: 45
unresolved unique dependencies: 41
```

Check at least:

* five resolved INVOKES dependencies;
* five resolved USES_TRANSFORMER dependencies;
* five unresolved built-in dependencies;
* five unresolved package-local or external dependencies.

Confirm:

* exact full-name matching;
* no fuzzy matching;
* no matching against backup artifacts;
* no matching against namespace folders;
* valid matching against Java Service metadata where appropriate;
* no case-insensitive false positives unless explicitly documented.

Assess whether one unique dependency can contain a mixture of resolved and unresolved occurrences. If that is impossible by design, state why.

## 8. Ordered FLOW tree audit

For the primary OAAdapter service verify:

```text
FLOW root: 1
SEQUENCE: 32
BRANCH: 27
LOOP: 2
EXIT: 9
```

Check that the tree:

* preserves exact child order;
* preserves nesting;
* does not flatten branch children;
* does not duplicate calls in both container and root collections unintentionally;
* places MAPINVOKE beneath the correct structural MAP;
* uses stable structural paths;
* retains unsupported MAP children without pretending they are fully parsed.

Select representative paths containing:

* nested SEQUENCE;
* BRANCH inside SEQUENCE;
* LOOP containing calls;
* MAP containing MAPINVOKE;
* EXIT in an error-handling path.

Compare each path directly with `flow.xml`.

## 9. SEQUENCE audit

Inspect all observed SEQUENCE attribute forms.

Report:

* attribute names found;
* canonical fields used;
* distinct exit-on values;
* labels or names;
* whether enabled/disabled information exists;
* whether any semantic interpretation such as TRY/CATCH was added.

Verify that the parser does not label sequences as TRY, CATCH or FINALLY without explicit evidence.

Check whether unknown attributes are retained or reported.

## 10. BRANCH and BRANCH_CASE audit

This is a critical section.

Inspect all 27 OAAdapter BRANCH elements.

Verify:

* each branch has exactly the expected number of case nodes;
* case order matches XML;
* case labels are preserved exactly;
* default cases are recognized only from real fixture evidence;
* branch switch values or expressions are extracted correctly;
* evaluate-label behavior is preserved where available;
* nested steps belong to the correct case;
* no normal child is incorrectly converted into a branch case;
* duplicate labels are not silently merged.

Report:

* branch count;
* total branch-case count;
* default-case count;
* branches lacking switch information;
* branches with unusual or ambiguous forms.

For at least five representative branches, compare the entire case structure directly with XML.

## 11. LOOP audit

Inspect both OAAdapter LOOP elements.

Verify:

* input path;
* output path if present;
* label;
* children;
* structural order;
* source reference;
* any canonical interpretation status.

Confirm that no array schema or runtime iteration behavior was inferred.

Report any LOOP attribute that exists in XML but is lost from IR.

## 12. EXIT audit

Inspect all nine OAAdapter EXIT elements directly.

For every EXIT report the observed form, grouped by unique attribute combination.

Verify:

* all nine are represented;
* source order and parent path are correct;
* declared scope is preserved;
* failure/signal information is preserved if present;
* messages or related metadata follow the project’s disclosure rules;
* no exception semantics are invented;
* valid EXIT elements no longer generate generic unsupported findings;
* unknown forms still produce findings.

Classify current EXIT interpretation as:

```text
CONFIRMED
PARTIALLY_INTERPRETED
RAW_ONLY
```

Report whether the canonical names accurately reflect what is actually demonstrated by the fixture.

## 13. Structural MAP handling

M2a was allowed to represent MAP only as a structural container.

Verify that:

* all 135 OAAdapter MAP elements are visible structurally or counted correctly;
* MAPINVOKE is attached to its containing MAP;
* mapping internals remain deferred to M2b;
* MAPCOPY, MAPSET, MAPDELETE, MAPSOURCE, MAPTARGET and DATA are not falsely represented as fully supported;
* deferred elements remain visible through findings or partial-support metadata;
* no mapping operation disappears silently.

Report the current finding counts for deferred mapping elements.

## 14. Source-reference audit

Sample at least:

* five SEQUENCE nodes;
* five BRANCH nodes;
* ten BRANCH_CASE nodes;
* both LOOP nodes;
* all nine EXIT nodes;
* five INVOKE occurrences;
* five MAPINVOKE occurrences;
* five unique dependencies.

Verify:

* relative forward-slash paths;
* exact XML path;
* correct source line where present;
* stable node or occurrence identifiers;
* no fabricated IDs;
* no absolute workspace paths.

Assess whether branch-case references are precise enough to distinguish cases with identical labels.

## 15. Markdown audit

Inspect the primary OAAdapter Markdown and at least two PGP Markdown files.

Verify that Markdown clearly separates:

```text
Normal service calls
Transformer calls
```

Check:

* occurrence counts;
* unique dependency counts;
* repeated-call counts;
* resolved status;
* FLOW overview;
* bounded hierarchy;
* indication of truncation;
* branch/loop/EXIT summaries;
* unsupported MAP semantics;
* no raw XML;
* no secret literal values;
* no absolute paths.

Check whether summaries are readable and whether technical facts can be traced to JSON/source evidence.

## 16. DOT audit

Inspect:

```text
out/m2a-analysis/graphs/dependencies.dot
```

Verify:

* exactly one edge per caller-target-kind;
* total edge count equals unique dependency count;
* `INVOKES` and `USES_TRANSFORMER` are distinguishable;
* occurrence counts are attached correctly;
* resolved status is represented consistently;
* no duplicate node definitions;
* unresolved targets remain nodes;
* LOW services remain represented;
* deterministic IDs;
* no absolute paths;
* no mapping values.

If Graphviz is installed, syntax-check the file. Its absence is not a failure.

## 17. Findings audit

Compare M1 and M2a findings behavior.

Verify:

* supported EXIT elements no longer create generic unsupported findings;
* typed control-flow elements do not also generate duplicate unsupported findings;
* deferred MAP internals remain explicitly partial/unsupported;
* unknown tags remain findings;
* malformed structures remain findings;
* finding aggregation does not hide malformed individual elements.

Report counts by finding code for the primary OAAdapter service.

## 18. Determinism audit

Run two clean analyses into separate temporary directories.

Compare byte-for-byte:

* output file list;
* `analysis.json`;
* every service Markdown file;
* `dependencies.dot`.

Report:

* number of files compared;
* aggregate hash;
* individual JSON and DOT hashes;
* any environment-dependent field;
* unstable IDs;
* unstable branch-case ordering;
* filename collisions.

## 19. Regression audit

Confirm M0 and M1 behavior remains available where intentionally retained:

* package inventory;
* artifact classification;
* secure XML handling;
* service identity;
* signatures;
* static calls;
* exact dependency resolution;
* classification;
* deterministic output.

Identify any M1 field removed or renamed without being documented in the schema migration notes.

## 20. Test-quality audit

Review tests for fixture-derived assertions.

Check coverage for:

* 108 call occurrences;
* 86 unique dependencies;
* dependency-kind separation;
* occurrence aggregation;
* branch-case structure;
* loop attributes;
* EXIT forms;
* ordered hierarchy;
* DOT unique edges;
* Markdown call separation;
* deterministic output;
* filename collision;
* fixture checksums.

Identify tests that merely repeat the implementation algorithm or only snapshot current output.

## 21. Scope audit

Confirm M2b has not been implemented.

M2b-only features include:

```text
typed MAPCOPY
typed MAPSET
typed MAPDELETE
MAPSOURCE/MAPTARGET relationship resolution
DATA interpretation
MAPINVOKE input/output mapping extraction
literal include/redact/omit policy
free-text extraction policy
mapping summaries
```

Incidental structural preservation is acceptable.

Production support for the above is out of scope for M2a.

## Required response

Return:

1. Executive summary.
2. Commands executed.
3. Repository baseline and scope.
4. Quality-gate results.
5. Schema-version audit.
6. Call-occurrence audit.
7. Unique-dependency audit.
8. OAAdapter dependency audit.
9. Resolution audit.
10. Ordered FLOW-tree audit.
11. SEQUENCE audit.
12. BRANCH and BRANCH_CASE audit.
13. LOOP audit.
14. EXIT audit.
15. Structural MAP audit.
16. Source-reference audit.
17. Markdown audit.
18. DOT audit.
19. Findings audit.
20. Determinism audit.
21. Regression audit.
22. Test-quality audit.
23. M2b scope-boundary audit.
24. Issues grouped as:

    * BLOCKER,
    * MAJOR,
    * MINOR,
    * INFORMATIONAL.
25. Final recommendation:

    * `GO FOR M2B`,
    * `GO FOR M2B WITH ACCEPTED MINOR ISSUES`,
    * `NO-GO: FIX M2A FIRST`.

For each issue include:

* evidence;
* affected code, fixture or output;
* why it matters;
* recommended correction;
* whether it must be fixed before M2b.

Do not modify files.

Do not fix issues.

Do not implement M2b.
