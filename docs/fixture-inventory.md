# Fixture Inventory Notes

Primary fixture:

- `samples/OriginalSmall/OAAdapter/ns/oa/adapter/geographicAddressManagement/createGeographicAddressValidation`
- Contains `node.ndf + flow.xml`
- `node.ndf` declares `svc_type=flow`
- Treated as the primary webMethods Integration Server 10.15 evidence for future milestones

Compatibility fixture:

- `samples/PGP`
- Contains `manifest.v3`, namespace folders, FLOW Services, Java Services, specifications,
  document types, Java source/classes, public resources, config, and key files
- Used as-is in this repository
- Upstream URL, exact commit SHA, and documented webMethods version are unknown

M0 output inventory is generated by:

```powershell
wm-doc scan samples --output out\inventory
```

M2b analysis output is generated by:

```powershell
wm-doc analyze samples --output out\m2b-analysis
```

Current analysis observations:

- OAAdapter primary service:
  `oa.adapter.geographicAddressManagement:createGeographicAddressValidation`
- OAAdapter signature contains one `input` document reference and one `output` document reference.
- OAAdapter observed FLOW tags include `FLOW`, `SEQUENCE`, `BRANCH`, `LOOP`, `INVOKE`, `MAPINVOKE`,
  `MAP`, `MAPCOPY`, `MAPSET`, `MAPDELETE`, `MAPSOURCE`, `MAPTARGET`, `DATA`, and `EXIT`.
- OAAdapter static invoke-like steps extracted: 43.
- OAAdapter static invoke targets are unresolved in the local sample snapshot because corresponding
  package metadata for adapter, `pub.*`, and related targets is not present.
- PGP active FLOW Services parsed by the same parser: 23.
- PGP active FLOW Services include static `INVOKE` and `MAPINVOKE` calls, but the observed sample set
  does not include `LOOP` or `EXIT`.
- PGP includes Java Service metadata that lets some static dependencies resolve without reading or
  executing Java bodies.

M2b observed active mapping tag counts:

- OAAdapter: 135 `MAP`, 190 `MAPCOPY`, 21 `MAPSET`, 8 `MAPDELETE`, 60 `MAPSOURCE`,
  60 `MAPTARGET`, 21 `DATA`, and 30 `MAPINVOKE`.
- PGP: 130 `MAP`, 107 `MAPCOPY`, 72 `MAPSET`, 170 `MAPDELETE`, 90 `MAPSOURCE`,
  90 `MAPTARGET`, 72 `DATA`, and 10 `MAPINVOKE`.

M2b canonical mapping counts:

- Total flow maps: 265.
- Total mapping operations: 568, split into 297 `COPY`, 93 `SET`, and 178 `DELETE`.
- Total MAPINVOKE transformer bindings: 198, split into 132 `INTO_TRANSFORMER` and
  66 `FROM_TRANSFORMER`.
- OAAdapter canonical mapping operations: 219, split into 190 `COPY`, 21 `SET`, and 8 `DELETE`.
- OAAdapter transformer bindings: 168, split into 112 `INTO_TRANSFORMER` and
  56 `FROM_TRANSFORMER`.

Observed mapping relationships:

- `MAPSOURCE` and `MAPTARGET` are direct children of `MAP` in current active fixtures and attach as
  map-level schema metadata summaries.
- `MAPCOPY`, `MAPSET`, and `MAPDELETE` are direct operation children of `MAP`.
- `MAPSET` literals use child `DATA` elements with `ENCODING="XMLValues"` and `I18N="true"` in the
  current fixtures.
- Current `MAPINVOKE` elements have child maps with `MODE="INVOKEINPUT"` and
  `MODE="INVOKEOUTPUT"`; bindings are extracted from those child map operations only as static
  evidence.
